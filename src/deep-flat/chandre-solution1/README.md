# Deep-Flat

Write a function that flattens nested arrays into a single array with no nesting

---

## Strategy

I plan to test using the array.join methods to remove the nesting test my
sandbox code in online Javascript compiler -

---

## Implementation

Will add once I begin researching solution I created a single array inside an
array and tested in REPL I then created a 3 array inside and array and then used
.map to loop through and do a .join to each array in the array. when this was
complete, coded solution and one test

---

## Use Cases

Use this to make data less complex.

---

## Inspiration

Will add once I begin researching solution
